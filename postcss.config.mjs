// Tailwind CSS v4 gebruikt de PostCSS-plugin. Geen tailwind.config nodig;
// thema staat in app/globals.css via @theme.
const config = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};

export default config;
